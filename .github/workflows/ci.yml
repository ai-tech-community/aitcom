name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    env:
      SKIP_ENV_VALIDATION: "true"

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm tsc --noEmit

      - run: pnpm exec vitest run src/server/db/apply-pending-migrations.test.ts

  format:
    name: Format Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm format:check

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, format]

    env:
      SKIP_ENV_VALIDATION: "true"
      DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://placeholder:placeholder@localhost:5432/placeholder' }}
      BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET || 'ci-placeholder-secret-ci-placeholder-secret' }}
      BETTER_AUTH_GITHUB_CLIENT_ID: "placeholder"
      BETTER_AUTH_GITHUB_CLIENT_SECRET: "placeholder"
      PAYLOAD_SECRET: ${{ secrets.PAYLOAD_SECRET || 'ci-placeholder' }}
      BETTER_AUTH_URL: "http://localhost:3000"
      BETTER_AUTH_BASE_URL: "http://localhost:3000"
      NEXT_PUBLIC_APP_URL: "http://localhost:3000"
      CRON_SECRET: "ci-placeholder"
      RESEND_API_KEY: "re_placeholder"
      S3_ACCESS_KEY_ID: "placeholder"
      S3_SECRET_ACCESS_KEY: "placeholder"
      S3_BUCKET: "placeholder"
      S3_REGION: "eu-central-1"

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm build
