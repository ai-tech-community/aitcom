"use client";

export function InsightsDashboard({ slug }: { slug: string }) {
  return (
    <div className="py-4 text-sm text-muted-foreground">
      Insights for {slug} — coming soon
    </div>
  );
}
