/** The root Hub community slug (the `ait` row every user belongs to). */
export const HUB_SLUG = "ait";

/** Directory / My Communities name. Matches seed-ait-community.ts (ADR-0019). */
export const HUB_NAME = "AIT Community";

/** Seed copy for the Hub root. The row is an anchor, not a tenant. */
export const HUB_DESCRIPTION =
  "The official AIT (AI Tech) community — where engineers, creators, and AI enthusiasts build the future together.";
